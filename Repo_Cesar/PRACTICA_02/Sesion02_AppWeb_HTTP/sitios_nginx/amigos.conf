server {
    listen 80;
    server_name localhost;

    # ESTA ES LA CLAVE: La barra / significa "todo"
    location / {
        proxy_pass http://amigos:5000;
        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
    }
}
